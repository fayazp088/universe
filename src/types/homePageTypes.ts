export type MenuItemsTypes = {
    title: string;
    imageUrl: string;
    size: string;
    linkUrl: string;
};

export type DirectorySectionTypes = {
    title: string;
    imageUrl: string;
    id: number;
    size?: string;
    linkUrl: string;
};
