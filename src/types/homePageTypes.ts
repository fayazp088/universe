export type MenuItemsTypes = {
    title: string;
    imageUrl: string;
    size: string;
};

export type DirectorySectionTypes = {
    title: string;
    imageUrl: string;
    id: number;
    size?: string;
};
