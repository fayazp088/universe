import { makeStyles } from '@mui/styles';

const directoryMenuStyles = makeStyles(() => ({
    directoryMenu: {
        width: '100%',
        display: 'flex',
        flexWrap: 'wrap',
        justifyContent: 'space-between',
    },
}));

export default directoryMenuStyles;
